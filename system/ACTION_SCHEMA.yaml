openapi: 3.1.0

info:
  title: AI Semiconductor Research OS GitHub API
  description: >-
    Reads the GitHub source of truth for study resume, progress, next topic, and
    paper continuation. Use for "이전 공부를 이어나가자", "어디까지 공부했어?",
    and "논문을 마저 읽자". Reads need no confirmation; Learning Log writes
    require approval.
  version: 0.2.3

servers:
  - url: https://api.github.com

security:
  - bearerAuth: []

paths:
  /repos/thisisjskim/ai-semiconductor-study/contents:
    get:
      operationId: listRepositoryRoot
      summary: List repository root
      description: >-
        Lists root files and directories only when an exact study path is unknown.
        Prefer getStudyPath for the mandatory learning-session bootstrap.
      parameters:
        - name: ref
          in: query
          required: false
          schema:
            type: string
            default: main
      responses:
        "200":
          description: Repository root contents
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/ContentItem"

  /repos/thisisjskim/ai-semiconductor-study/contents/{path}:
    get:
      operationId: getStudyPath
      summary: Read the current study state or list a study directory
      description: >-
        For "이전 공부를 이어나가자" and every learning request, first call once
        with path=state/CURRENT_LEARNING_CONTEXT.md and ref=main, then begin from
        its Current Topic and Next Action. Read other files only when exact
        evidence is needed.
      parameters:
        - name: path
          in: path
          required: true
          description: Repository-relative file or directory path.
          schema:
            type: string
        - name: ref
          in: query
          required: false
          schema:
            type: string
            default: main
      responses:
        "200":
          description: File content or directory listing
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/FileContent"
                  - type: array
                    items:
                      $ref: "#/components/schemas/ContentItem"
        "404":
          description: File or directory does not exist

  /repos/thisisjskim/ai-semiconductor-study/issues:
    post:
      operationId: createLearningLogIssue
      summary: Start an approved Learning Log submission
      description: Creates one plain-text Issue after user approval. Put the ingest envelope and first Markdown chunk in body.
      x-openai-isConsequential: true
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title, body]
              properties:
                title:
                  type: string
                  description: Must be [learning-log] YYYY-MM-DD topic-slug.
                body:
                  type: string
                  description: Ingest envelope followed by the first plain Markdown chunk. Keep each chunk below 30000 characters.
      responses:
        "201":
          description: Issue created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Issue"

  /repos/thisisjskim/ai-semiconductor-study/issues/{issue_number}/comments:
    get:
      operationId: listLearningLogIssueComments
      summary: Check Learning Log processing results
      description: Lists Issue comments to verify the GitHub Actions success or failure result.
      parameters:
        - name: issue_number
          in: path
          required: true
          description: Issue number returned by createLearningLogIssue.
          schema:
            type: integer
      responses:
        "200":
          description: Issue comments
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: "#/components/schemas/IssueComment"
    post:
      operationId: appendLearningLogChunk
      summary: Append the next Learning Log chunk
      description: Appends plain Markdown to an existing approved Issue. Preserve order and keep each chunk below 30000 characters.
      x-openai-isConsequential: true
      parameters:
        - name: issue_number
          in: path
          required: true
          description: Issue number returned by createLearningLogIssue.
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [body]
              properties:
                body:
                  type: string
                  description: Next plain Markdown chunk only. Do not repeat the ingest envelope.
      responses:
        "201":
          description: Comment created
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IssueComment"

  /repos/thisisjskim/ai-semiconductor-study/issues/{issue_number}:
    get:
      operationId: getLearningLogIssue
      summary: Read a Learning Log submission Issue
      description: Reads the Issue state and URL before or after processing.
      parameters:
        - name: issue_number
          in: path
          required: true
          description: Issue number returned by createLearningLogIssue.
          schema:
            type: integer
      responses:
        "200":
          description: Issue details
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Issue"
    patch:
      operationId: closeLearningLogIssue
      summary: Finish an approved Learning Log submission
      description: Closes the Issue after all chunks are appended. Closing triggers GitHub Actions to create or update the file.
      x-openai-isConsequential: true
      parameters:
        - name: issue_number
          in: path
          required: true
          description: Issue number returned by createLearningLogIssue.
          schema:
            type: integer
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [state]
              properties:
                state:
                  type: string
                  enum: [closed]
                  default: closed
      responses:
        "200":
          description: Issue closed and processing queued
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Issue"

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer

  schemas:
    ContentItem:
      type: object
      properties:
        name: { type: string }
        path: { type: string }
        sha: { type: string }
        type: { type: string }
        size: { type: integer }
        html_url: { type: string }
        download_url:
          type: [string, "null"]

    FileContent:
      type: object
      properties:
        name: { type: string }
        path: { type: string }
        sha: { type: string }
        size: { type: integer }
        type: { type: string }
        content:
          type: string
          description: Base64 returned by GitHub for reads only. Never reuse it for writes.
        encoding: { type: string }
        html_url: { type: string }

    Issue:
      type: object
      properties:
        number: { type: integer }
        title: { type: string }
        state: { type: string }
        body: { type: [string, "null"] }
        html_url: { type: string }

    IssueComment:
      type: object
      properties:
        id: { type: integer }
        body: { type: [string, "null"] }
        html_url: { type: string }
        user:
          type: object
          properties:
            login: { type: string }
